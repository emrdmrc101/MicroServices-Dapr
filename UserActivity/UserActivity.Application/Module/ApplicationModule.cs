using Autofac;
using Core.Modules;

namespace UserActivity.Application.Module;

public class ApplicationModule : BaseModule
{
    protected override void Load(ContainerBuilder builder)
    {
        base.Load(builder);
    }
}